// 1. --> Logical AND (&&) true only if both values are true otherwise false

const a = true;
const b = false;
const c = 4;

// console.log(a && b);
// console.log(c > 2 && c < 2)

// 2. --> Logical OR (||) true if either value is true only false when both are false

// console.log(a || b);
// console.log(c > 2 || c < 2)

// 3. --> Logical NOT (!) true gets converted to false and vice-versa

console.log(!a);
console.log(!c);

